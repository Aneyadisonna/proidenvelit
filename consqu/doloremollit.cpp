char letter = 'Q'; // Example variable

switch (letter) {
    case 'Q':
        // Code to execute if letter is 'Q'
        break;
    case 'W':
        // Code to execute if letter is 'W'
        break;
    case 'E':
        // Code to execute if letter is 'E'
        break;
    // Additional cases as needed
    default:
        // Code to execute if none of the above cases are matched
        break;
}
